version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: usebarber
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: usebarber
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U usebarber"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: postgresql://usebarber:${POSTGRES_PASSWORD}@postgres:5432/usebarber
      AUTH_SECRET: ${AUTH_SECRET}
      AUTH_URL: ${AUTH_URL}
      AUTH_TRUST_HOST: "true"
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET}
      PAGARME_SECRET_KEY: ${PAGARME_SECRET_KEY}
      NEXT_PUBLIC_PAGARME_PUBLIC_KEY: ${NEXT_PUBLIC_PAGARME_PUBLIC_KEY}
      PAGARME_WEBHOOK_SECRET: ${PAGARME_WEBHOOK_SECRET}
      PAGARME_PLATFORM_FEE_PERCENT: ${PAGARME_PLATFORM_FEE_PERCENT}
      PAGARME_PLATFORM_RECIPIENT_ID: ${PAGARME_PLATFORM_RECIPIENT_ID}
      ASAAS_URL: ${ASAAS_URL}
      ASAAS_API_KEY: ${ASAAS_API_KEY}
      ASAAS_WEBHOOK_TOKEN: ${ASAAS_WEBHOOK_TOKEN}
      GEMINI_API_KEY: ${GEMINI_API_KEY}
      VAPID_PUBLIC_KEY: ${VAPID_PUBLIC_KEY}
      VAPID_PRIVATE_KEY: ${VAPID_PRIVATE_KEY}
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  postgres_data:
